####################################
#### NEW CONTENT ELEMENT WIZARD ####
####################################
mod.wizards.newContentElement.wizardItems.interactive {
    elements {
        googlemaps {
            title = Google Maps
            iconIdentifier = content-bootstrappackage-card-group
            description = add a googlemaps iframe to the page
            tt_content_defValues {
                CType = googlemaps
            }
        }
    }
    show := addToList(googlemaps)
}
